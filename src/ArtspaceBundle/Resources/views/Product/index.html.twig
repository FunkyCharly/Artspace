{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Product list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categorie</th>
                <th>Detail</th>
                <th>Prix</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('backoffice_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.categorie }}</td>
                <td>{{ entity.detail }}</td>
                <td>{{ entity.prix }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('backoffice_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('backoffice_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('backoffice_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
