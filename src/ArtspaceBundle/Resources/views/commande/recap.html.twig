{% block body %}
    <table>
        
    {% for c in commande %} 
        <tr>
        <td>{{ c.id }}</td>
    <td>{{ c.username }}</td>
    <td>{{ c.email }}</td>
    <td>{{ c.Datecreated |date("d-m-Y H:i:s") }}</td>
    <td>{{ c.total }}</td>
        </tr>
   
   {% endfor %} 
    </table>
    {% endblock body %}